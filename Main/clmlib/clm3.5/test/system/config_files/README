vo    => voc  is on
ds    => dust is on
r     => rtm  is on

_dh   => debug on, hybrid   on
_dm   => debug on, mpi only on 
_do   => debug on, omp only on

_h    => debug off, hybrid   on
_m    => debug off, mpi only on
_o    => debug off, omp only on
